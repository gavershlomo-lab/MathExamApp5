# דוח בדיקה — גרסה 5.4 (מנוע שרטוטים)

## מה היה הבעיה ב-5.3
השרטוט נבנה ישירות מה-JSON ש-Gemini החזיר, והשער היחיד היה ציון הביטחון שהמודל דיווח על עצמו (≥0.85).
לא הייתה השוואה לנוסח השאלה, לא בדיקה גאומטרית, לא השוואה למקור ולא אישור מורה.

## מה נבנה
צינור: תמונה → JSON מובנה → מיזוג עם נוסח השאלה → פתרון אילוצים → אימות קשיח → ציור דטרמיניסטי →
השוואה מבנית → החלטה → אישור מורה. פירוט בקובץ README ובקובץ CHANGELOG.

## בדיקות שהורצו (בסביבת הפיתוח)
| בדיקה | תוצאה |
|---|---|
| `tests/test_diagram_engine.py` — שורשים ממשיים, אבטחת המפענח, גרפים (ליניארי, ריבועי, שלישי, שורש, שורש שלישי, ערך מוחלט, תחום שלילי, רציונלי עם חור ואסימפטוטה, מוגדרת בחלקים), נקודה על עקומה, מבחני קבלה 1–7, אילוצים (שווה שוקיים, זווית ישרה, מקבילות, אמצע, מעגל-מיתר-משיק), אילוצים סותרים, היפוך צד, "לא לפי קנה מידה", חורים בוולידציה, תרשימים, אנטי-הזיה, דטרמיניזם, אישור/עריכה, עמידות לקלט שבור, תאימות ל-5.x, חבילת ביקורת | 77 עברו |
| `tests/test_core.py` — כולל מבחן קבלה 10 (שחזור נכנס למסמך רק אחרי אישור) ושרטוט שבור לא מפיל מסמך | 86 עברו |
| `tests/test_app.py` — כפתורי הביקורת, עריכת גרף (מבטלת אישור), עורך גאומטריה | 6 עברו |
| סה"כ הרצה משותפת | 169 עברו |
| בדיקת דפדפן מלאה (Gemini מדומה): חיתוך→מחיקה, מחיקה→יישור→חיתוך→ביטול→חיתוך, בדיקת פיקסלים במחיקה, חיתוך ברזולוציה מלאה, התמונה הערוכה היא שנשלחה לפענוח, אישור שחזור בדפדפן, 7 הורדות | עברה |
| אותה בדיקה במצב שכבה חינמית (2 קריאות בדקה) | ראו סיכום |
| ולידציית XSD לשלושת קובצי ה-Word | עברה |
| PDF: עברית מימין לשמאל, נוסחאות, שחזור מאושר בשאלה 1 ומקור בשאלה 2 | נבדק בעין |

## לא נבדק (NOT TESTED)
- Gemini אמיתי: לסביבת הבדיקה אין גישה לשרתי Google. לא ידוע עדיין כמה טוב המודל האמיתי ממלא את המבנה החדש (במיוחד `observed` ו-`labels`). אם הוא ממלא חלקית — המערכת תהיה שמרנית יותר (יותר "טיוטה"/"מקור"), לא פחות בטוחה.
- פתיחה ב-Microsoft Word (נבדק ב-LibreOffice ובוולידציית סכמה בלבד).
- Streamlit Community Cloud (הבדיקות רצו מקומית עם אותן גרסאות חבילות).

## מגבלות ידועות
- **אין חילוץ עקומה מפיקסלים.** בלי נוסחה בשאלה העקומה נשארת "מהתמונה", הביטחון מוגבל ל-0.85, והמורה חייב לאשר או לתקן.
- **אין מנוע OCR מקומי.** התוויות מגיעות מ-Gemini עם רמת ביטחון וחלופות; תווית לא ודאית חוסמת עד שהמורה מתקן.
- **אין גרירת נקודות בעכבר.** העריכה נעשית בטבלאות (קואורדינטות, קטעים, סימונים, אילוצים).
- פונקציות מוגדרות בחלקים לא מזוהות מנוסח השאלה (רק מהמבנה שהמודל החזיר או מעריכת המורה).
- ערכי זוויות שאינם 90° מוצגים כתווית ואינם נאכפים גאומטרית (כדי לא לעוות שרטוט שאינו לפי קנה מידה).
- ציון הדמיון החזותי הוא מידע בלבד (סגנון, רשת וקנה מידה שונים מהמקור); ההחלטה נשענת על ההשוואה המבנית.
- ב-Word נשתל PNG ב-300dpi ולא SVG (SVG נשמר בחבילת הביקורת).
- ללא נתוני `observed` מהמקור ההשוואה חלקית, ולכן הביטחון יורד והמערכת מציעה טיוטה או מקור.
