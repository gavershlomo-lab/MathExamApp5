# דוח ביקורת קוד — מחולל מבחנים 5.2-audited

הבדיקה בוצעה על גרסה 5.1 שהתקבלה כ-ZIP. המטרה הייתה לזהות תקלות, להפחית זמני rerun, ולחזק את נכונות הפתרון המתמטי ואת מנגנון ה-fail-closed לפני הפקת מסמכים.

## תקלות מהותיות שנמצאו ותוקנו

1. **שורש אי-זוגי בתחום השלילי** — `sqrt[3](x)` ו-`x^(1/3)` הומרו ל-NumPy באופן שהחזיר `NaN` עבור `x<0`. התיקון ממיר חזקות רציונליות בעלות מכנה אי-זוגי ל-`sympy.real_root` לפני `lambdify`.
2. **אימות עצמאי ריק עבר בטעות** — `items=[]` יחד עם `overall_agrees=true` היה יכול לעבור ולידציה. כעת נדרש כיסוי מדויק של כל הסעיפים.
3. **כשל אימות היה רק אזהרה** — כעת כשל באימות חוסם הפקה, אלא אם המורה מאשר ידנית.
4. **תשובה סופית חסרה** — בעבר סעיף עם שלבי פתרון אך ללא `final_answer` יכול היה לעבור. כעת הוא חוסם הפקה.
5. **הבודק בדק בעיקר תשובות סופיות** — כעת מועברים אליו כל שלבי הפתרון והוא נדרש לבדוק גם התאמת מקור וגם תקפות דרך.
6. **מירוץ במעבר בין מודלי Gemini** — worker מאוחר היה יכול לסמן ככושל מודל אחר מזה שבאמת נכשל. המעבר מקבל כעת במפורש את `failed_model`.
7. **תקיעות/איטיות בחיתוך ובמחיקה** — עורך התמונה הועבר ל-`st.fragment`, תצוגת העבודה נשמרת ב-cache, והקנבס עודכן מ-0.9.3 ל-0.13.0 ללא monkey-patch פנימי של Streamlit.
8. **כל לשוניות שלב 4 הורצו בכל rerun** — הוחלפו בבורר שמרנדר רק את השאלה הנבחרת.
9. **שחזור שרטוט בביטחון נמוך נכנס למסמך** — כעת מתחת ל-0.85 נבחר מקור התמונה אוטומטית.
10. **תמונה שנמחקה לא הייתה ניתנת להעלאה מחדש** — hash התמונה מוסר כעת מרשימת `consumed` בעת מחיקה/ניקוי.
11. **ZIP לא נדחס בפועל** — `ZipInfo` קיבל `compress_type=ZIP_DEFLATED` במפורש.
12. **צריכת זיכרון בעריכת תמונות** — עומק ה-undo לתמונות מלאות צומצם מ-6 ל-3.

## חיזוקי דיוק מתמטי

- פתרון ראשוני ואימות עצמאי מבקשים `thinking_level="high"`.
- אימות דורש שלושה צירים: התאמת נוסח/נתונים לצילום, תקפות דרך הפתרון, ושוויון התשובה הסופית.
- לכל סעיף נדרשת תשובה סופית מפורשת.
- שורשים אי-זוגיים אמיתיים נבדקים גם בתחום השלילי.
- שחזור תרשים אינו גובר על המקור כאשר ביטחון ה-AI נמוך.

## מה עדיין לא ניתן להבטיח באופן מתמטי

מערכת המבוססת על מודל גנרטיבי אינה יכולה להבטיח אפס טעויות בכל סוגי שאלות הבגרות. שתי קריאות לאותו מודל עלולות לחלוק אותה טעות. לכן בגרסה זו המערכת מחמירה וחוסמת כשבדיקה חסרה/נכשלת, אך לבחינה בעלת משמעות רשמית מומלץ להשאיר אישור מורה סופי. לשכבת אמינות גבוהה עוד יותר כדאי בהמשך להוסיף verifier דטרמיניסטי מבוסס SymPy לפי סוג שאלה (אלגברה/חדו"א/טריגונומטריה) ולא להסתמך רק על LLM.

## בדיקות שבוצעו בסביבת הביקורת

- `py_compile` עבר עבור `app.py`, `exam_core.py` וכל קבצי הבדיקות.
- 19 בדיקות ממוקדות עברו, כולל ניתוח mock מלא, disagreement, fail-closed ושלל בדיקות parser; התוצאה: **19 passed**.
- בדיקות ידניות חישוביות אישרו: `(-8)^(1/3)=-2`, `(-8)^(2/3)=4` ו-`(-10)^(1/3)` מחושב כמספר ממשי.
- סביבת הביקורת עצמה אינה כוללת Streamlit, google-genai, latex2mathml ו-mathml2omml, ואין לה גישה לרשת להתקנתם. לכן לא ניתן היה להריץ כאן את סוויטת E2E בדפדפן או את כל בדיקות Word/PDF מול התלויות האמיתיות. יש להריץ את הפקודות שב-README בסביבת הפרויקט/Streamlit Cloud לפני העלאה לפרודקשן.
